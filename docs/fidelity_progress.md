# シミュレーション忠実度(exact化)進捗

目標: 全5,178枚を exact(現実と1対1)に模擬する。

## 進捗推移(本セッション)

| 時点 | exact | 備考 |
|---|---|---|
| 開始 | 45 (0.9%) | 手書きのみ |
| 静的キーワード | 173 | バニラ+ブレイカー/ブロッカー/SA等 |
| ETB/Sトリガー基本 | 367 | 破壊/ドロー/ハンデス/タップ/バウンス/マナ送り |
| トリガー拡張+条件 | 379 | 攻撃時/破壊時、マナ武装/革命条件 |
| 攻撃制限+枚目+捨てさせる | 522 | ブロッカー(攻撃できない)等 |
| on_attack汎用+墓地回収 | 592 | 種別フィルタ付きゾーン移動 |
| look_and_take | 604 | 探索/インパルス(新op) |
| modify_power | 640 | 一時パワー修整(新op) |
| modify_power+aura+置換 | 710-825 | キーワード付与・置換効果・複文継続 |
| **現在** | **825 (15.9%)** | |

## 実装した忠実パーサ(exact-safe・全節カバー方式)

`src/battle/effects/exact_parser.py`。設計原則:
- カード全文を節に分割し、各節を既知アクション or 模擬済み静的キーワードに対応づける。
- **全節を説明できた場合のみ exact**。1節でも未知/条件不明/曖昧なら reject。
- これにより「exactと記録したのに未模擬」「条件付きを無条件適用(過大評価)」を構造的に防ぐ。

対応済み: 破壊/ドロー/ハンデス/自己ディスカード/タップ/アンタップ/バウンス/マナ送り/
蘇生/手札召喚/シールド操作/墓地回収(種別)/マナ加速/自己ミル/相手ミル/パワー修整/
look_and_take(探索)、条件(マナ武装/革命/墓地枚数/シールド数)、トリガー(ETB/攻撃時/破壊時/Sトリガー)。

## 追加した engine op / 機構
destroy_mana, destroy_creatures_nonciv, target_filter(blocker), look_and_take,
modify_power(一時パワー+ターン終了リセット+0以下破壊), deck_top_to_grave(scope=opponent),
grave_to_hand/grave_to_mana(card_filter), cannot_attack(_player)静的化。

## 残り(約4,538枚)が exact 化に要するもの

自動パーサで安全に取れる単純パターンは概ね尽きた。残りは:
1. **オーラ/キーワード付与系(約106枚)**: 「自分の〜は『SA/ブロッカー』を得る」。
   engineの静的プロパティ読取(is_speed_attacker等)がバトルゾーンのオーラを参照する
   仕組みが必要。combat中核に触れるため要慎重実装。
2. **コスト変動(約176枚)**: 種族限定の召喚/呪文コスト増減。effective_costに種族
   フィルタ対応が必要(無条件分は既に模擬済み)。
3. **特殊キーワード**: B・A・D/連鎖/侵略/革命チェンジ/ニンジャ/ナイト・マジック/
   無月の門 等。各々が固有の召喚・誘発ルール。
4. **未模擬条件**: 最大マナ比較/手札枚数/引いたカードの文明/特定名カード参照 等。
5. **その他固有効果**: 強制バトル/置換効果(破壊されるかわりに〜)/マナ召喚 等。

これらは「DMルールエンジンの作り込み」であり、1機構ずつエンジン実装+テストを積む
長期作業。本セッションで土台(exact-safeパーサ+主要op+12.4%)を構築した。

## セッション到達点(追記): exact 838 (16.2%)

このセッションで 0.9%→**16.2%**(45→838枚)。実装した engine 機構/トリガー:
- op: destroy_mana, destroy_creatures_nonciv, look_and_take, modify_power,
  target_filter(blocker), source-target(このクリーチャー), deck_top_to_grave(opponent),
  grave_to_hand/mana(card_filter), summon_from_mana/hand。
- トリガー: on_play/on_cast/on_attack/on_destroyed/s_trigger に加え、
  **on_spell_cast(呪文を唱えた時)/on_turn_start(ターン開始時)/on_turn_end(ターン終了時)/
  on_win(バトルに勝った時)** を追加。
- 機構: キーワード付与オーラ(SA/ブロッカー/スレイヤー)、置換効果(破壊されるかわりに〜)、
  一時パワー修整(ターン終了リセット・0以下破壊)、ブロッカー限定除去。
- 条件: マナ武装/革命/墓地枚数/シールド数(以上・以下)/手札枚数。
- 構文: 複文の継続節トリガー継承、任意形(してもよい)正規化、「そうした場合」逐次化、
  G・G・Gラベル除去、複合節分解、取りこぼし検出(残余アクション)。

## 残り(約4,340枚)= 個別実装が必要な裾野
特殊キーワード(無月の門/侵略/革命チェンジ/ニンジャ/B・A・D/連鎖/メクレイド/
ナイト・マジック)、相対条件(最大マナ/シールド数 vs 相手)、群パワーオーラ、強制バトル、
固有複合効果。各々が固有ルールで、1機構ずつエンジン実装+テストの長期作業。
exact-safe原則(全節カバー・過大評価排除)は厳守しており、未対応カードは approx/draft の
ままで「偽exact」は生んでいない。
